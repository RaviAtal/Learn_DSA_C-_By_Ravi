// Q1. Calculating percentage of 5 subjects

#include <iostream>
using namespace std;

int main()
{
    int a, b, c, d, e;
    cin >> a >> b >> c >> d >> e;
    float p = (a + b + c + d + e) / 5;
    cout << "percentage of 5 sub :" << p << " % " << endl;
}